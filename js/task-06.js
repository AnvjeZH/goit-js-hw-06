rconst inputEl = document.getElementById('validation-input')
inputEl.addEventListener('blur', onValidation)

function onValidation(event) {
      const cur
    if (event.target.value.lenght === 6) {
          console.log('It' );  
    }
    return console.log('Not')
    }

